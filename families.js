const family = require("./family.json");

const deepestMember = (family) => {
  // Write code to find the youngest member of the family tree.
  return family;
};

console.log(deepestMember(family.families));
